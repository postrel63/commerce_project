server:
  port: 8081
feign:
  okhttp:
    enabled: true

#spring.mvc.pathmatch.matching-strategy=ant_path_matcher
spring:
  mvc:
    path match:
      matching-strategy: ant_path_matcher

  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driverClassName: org.h2.Driver
  h2:
    console:
      enabled: true

#  datasource:
#    url: jdbc:mysql://localhost:3306/zeroorder
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    username: root
#    password: 1

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

